services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: ai_mail
      POSTGRES_USER: ai_mail_user
      POSTGRES_PASSWORD: ai_mail_pass
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

  redis:
    image: redis:7
    ports:
      - "6379:6379"

volumes:
  pgdata:
